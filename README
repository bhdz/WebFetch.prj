#!/usr/bin/cat2

:Hello
::Basically {A Global Web Data Fetcher: In other words, wget lib for python + mirror sh1t}
 ::Hello World {of; Python's Non-Class OOP mixed with FLAT hierarchy Experiment}
  ::I {: Provide; &Provider &&Of; Library;; That [HAS] FLAT class(object) :: hierarchy}
   :: WILL {: Provide; The Basic &for &&a new :: DAWN!? In History LOL I AM THAT BIA... Whta tod you care for ... ~ . ~ WHAT ~ IS ~ YOUR QUEST ~ . ~ }
